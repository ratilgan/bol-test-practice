name: Playwright Tests
on:
  push:
    branches: [main, master]
  #pull_request:
  #  branches: [main, master]
  schedule:
    # Run every day at 06:00 CET (05:00 UTC)
    - cron: "0 5 * * *"
  workflow_dispatch:
jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
      - name: Install dependencies
        run: npm ci
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
      - name: Run Playwright tests
        run: npx playwright test

       # 6. Show report folder
      - name: Show playwright-report folder
        if: always()
        run: ls -R ./playwright-report

      # 7. Publish Playwright HTML report to GitHub Pages
      - name: Publish report to GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v3.9.3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./playwright-report
          
      - uses: actions/upload-artifact@v4
        if: ${{ !cancelled() }}
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
      - name: Send test results via email
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: ${{ secrets.EMAIL_SERVER }}
          server_port: ${{ secrets.EMAIL_PORT }}
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          subject: "Playwright Tests Results - ${{ job.status }} - ${{ github.run_number }}"
          to: ${{ secrets.COLLABORATOR_EMAILS }}
          from: ${{ secrets.EMAIL_FROM }}
          body: |
            Test Run: ${{ github.run_id }}
            Status: ${{ job.status }}
            Branch: ${{ github.ref }}
            Commit: ${{ github.sha }}

            Failed again, FUCK ME !!!

            View Full Report: https://ratilgan.github.io/bol-test-practice/